# Convex-hosted environment values used by the current local applications.
# These are deployment settings, not values loaded from `.env.local`.
# Keep secrets blank in Git. Existing project members can retrieve the current
# connector secret through `bun run setup:env` after authenticating with Convex.

BETTER_AUTH_SECRET=
CONNECTOR_SIGNING_SECRET=
SITE_URL=https://perceptive-grasshopper-340.convex.site
WEB_SITE_URL=http://localhost:3000
PWA_SITE_URL=http://localhost:5173
