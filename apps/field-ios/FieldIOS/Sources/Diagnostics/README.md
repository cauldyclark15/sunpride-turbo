# Diagnostics

`DeviceDiagnostics` — non-secret model / iOS / app version / key-storage facts for the enrollment screen. DEBUG-only `PublicKeyExport` writes the **public** key to `Documents/device-public-key.txt` (refuses anything that is not a 91-byte SPKI).
